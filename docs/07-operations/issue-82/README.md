# VARSHAVKA menu cards — draft v2.0.0

Open `VARSHAVKA_MENU_COSTING_TECH_CARDS_DRAFT_v2.0.0.xlsx` and start with `00_ПАСПОРТ`, then review `15_ПРОВЕРКИ`.

This workbook is a controlled draft, not a release-ready recipe or safety document. It contains exactly 28 in-scope dishes and 17 sheets. The accepted economic basis is HOF-0005 v0.2.1 only.

## Editing rules

1. Enter evidence-backed changes only in yellow input cells.
2. Never replace an unknown blank with zero.
3. Do not remove `BLOCK` safety vetoes or populate nutrition figures without a new accepted handoff.
4. Do not populate complete COGS, channel price, food cost or margin until all required costs are accepted.
5. Use Change Request control for recipe, VSF, safety or pricing changes.
6. Rebuild with `releases/builds/build_issue_82_menu_cards.mjs`, then rerun formula, external-recalc and visual QA.

Gate D verdict: `PASS_WITH_CONDITIONS`.
